# Tmux

## 링크

```bash
ln -sfn ~/workspaces/sharosoo/dotfile/tmux/.tmux.conf ~/.tmux.conf
tmux source-file ~/.tmux.conf
```

## TPM (플러그인)

[TPM](https://github.com/tmux-plugins/tpm) 설치 후 tmux 안에서 **prefix + I**.

`install.sh`에 있던 자동 TPM 설치는 제거됨 — 필요 시 공식 README 따름.